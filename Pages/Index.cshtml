@page
@model IndexModel
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@{
    ViewData["Title"] = "Home page";
}

<div class="p-6">
    <h1 class="text-2xl font-semibold">
        Welkom @Model.Email
        <span class="text-gray-500 font-normal">(@Model.RoleLabel)</span>
    </h1>
</div>
